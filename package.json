{
  "name": "mentorship-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix functions",
    "build": "npm install --prefix functions && npm run build --prefix functions",
    "start": "node functions/lib/server.js",
    "test": "npm test --prefix functions"
  }
}
